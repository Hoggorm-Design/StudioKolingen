
const Carousel = () => {
    return (
        <section>
            <img
                src=""
                alt="Will need to be mapped such that carousel images will be displayed sequentially"
            />
            <div>
                <button>Left</button>
                <button>Right</button>
            </div>
        </section>
    );
};

export default Carousel;
