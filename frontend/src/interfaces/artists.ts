export interface Artist {
    image: {
        asset: {
            _ref: string;
            url: string;
        };
    };
    header: string;
    alt: string;
    text: string;
    link: string;
}
