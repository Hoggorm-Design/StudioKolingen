export interface Logo {
    mainLogo: {
        asset: {
        _ref: string;
        url: string;
        }
        altText: string;
    };

  footerLogo: {
    asset: {
      _ref: string;
      url: string;
    }
    altText: string;
  };
}